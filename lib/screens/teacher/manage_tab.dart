import 'package:flutter/material.dart';

class TeacherManageTab extends StatelessWidget {
  const TeacherManageTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Teacher Manage')),
      body: const Center(child: Text('Teacher Manage Tab (Mockup)')),
    );
  }
}
